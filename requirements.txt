# Python 3.10-3.12 (developed on 3.12)
# Install:  pip install -r requirements.txt
#
# Note: build the virtualenv on a standalone Python, not on a conda
# interpreter. A venv layered over conda inherits conda's OpenMP/BLAS
# libraries, which clash with the ones bundled in the TensorFlow and
# XGBoost wheels and segfault on import (macOS especially).

numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0
tensorflow>=2.16        # Apple Silicon is supported by this package from 2.16;
                        # the old tensorflow-macos package is retired.
statsmodels>=0.14       # variance inflation factors in feature_selection.py
matplotlib>=3.8
seaborn>=0.13
openpyxl>=3.1           # reads dataset/data_structures.xlsx
